# System-Level Scripts

This folder is split into:

- `diagnosis/`
- `forecasting/`

Use `diagnosis/` for system-level diagnostic entrypoints and `forecasting/` for runnable system-level forecast pipeline entrypoints.

